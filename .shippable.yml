language: c
build:
  pre_ci:
    - mkdir mybuild
    - cp ./tests/docker/dockerfiles/debian-multiarch-cross.docker mybuild/Dockerfile
    - docker build -t qemu:debian-multiarch-cross mybuild
  pre_ci_boot:
    image_tag: qemu:debian-multiarch-cross
    pull: false
    options: "-e HOME=/root"
  ci:
    - sudo apt-get update -qq
    - apt-get build-dep -a armhf -qq qemu
    - ./configure --cross-prefix=arm-linux-gnueabihf-
    - make -j3
    - make test
